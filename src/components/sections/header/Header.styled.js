import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  top: 0;
  width: 1080px;
  height: 4rem;
  z-index: 10;
  box-shadow: 0 6px 10px -10px rgba(1, 1, 1, 0.3);
  background-color: #8db4a7;
  display: flex;
  align-items: center;
  justify-content: center;
`;
